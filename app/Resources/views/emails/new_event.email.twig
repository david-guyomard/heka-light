{% block body_text %}
{% autoescape false %}

    Une réservation vient d'être effectuée :
    {{ app.user.username }}
    a réservé pour "{{ type_reservation }}" <br/>
    le {{ date_reservation|date('d/m/Y H:i:s') }}
    à l'adresse suivante : {{ address_reservation }}

{% endautoescape %}
{% endblock %}